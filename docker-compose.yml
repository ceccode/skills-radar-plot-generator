services:
  app:
    build:
      context: .
    volumes:
      - .:/app
    working_dir: /app
    command: python main.py

  tests:
    build:
      context: .
    volumes:
      - .:/app
    working_dir: /app
    command: python -m unittest discover -s tests -p "test_*.py"